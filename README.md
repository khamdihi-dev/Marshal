# Marshal
Simpel tools untuk enc ( biar gak di recode )

# install
    $ pkg install git
    $ pkg install python
    $ git clone https://github.com/khamdihi-dev/Marshal
    $ cd Marshal
    $ python Enc.py
